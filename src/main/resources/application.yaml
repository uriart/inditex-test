spring:
  application:
    name: Inditex-demo

  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password:
    platform: h2

  jpa:
    hibernate:
      ddl-auto: none

  h2:
    console:
      enabled: true
